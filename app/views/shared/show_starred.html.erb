<% provide(:title, "Starred Lists") %>
<h1 class="left"><%=link_to "#{@user.name}'s", user_path(@user)%>  Starred Lists</h1>
<div class="container">
  <ol class="lists">
    <%=render @lists%>
  </ol>
</div>
