<% provide(:title, "Starred Lists") %>
<h3 class="left"><%=link_to "#{@user.name}'s", user_path(@user)%>  Starred Lists</h3>
<div class="container">
  <ol class="lists">
    <%=render @lists%>
  </ol>
</div>
