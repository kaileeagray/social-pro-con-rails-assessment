<% if @feed_items.any? %>
  <ol class="lists">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items, class:"text-center" %>
<% end %>

<%= link_to 'New List', new_list_path, class:"right btn btn-primary"%>
